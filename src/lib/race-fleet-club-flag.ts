/**
 * `race_fleets` keeps a placeholder `ics_signal` for DB constraints; on-water class
 * hoists use the club fleet `class_flag` from Club admin settings.
 */
export const RACE_FLEET_ICS_PLACEHOLDER = "p";
